# Required for GitHub REST API calls (repo create, secrets, dispatch).

# Optional but recommended:
ZEN_API_KEY=
GROQ_API_KEY=
NVIDIA_API_KEY=
NIM_MODEL=stepfun-ai/step-3.7-flash
AGENT_TOKEN=
WEBHOOK_SECRET=
WORKER_URL=
LLM_PROVIDER=mock|zen|groq|nim

# Optional: JSON file containing remote HTTP and stdio MCP server definitions.
# See mcp.servers.example.json. Keep real connection values out of this file.
MCP_CONFIG=
MCP_SERVERS_JSON=
GOAL_APPROVAL_TOKEN=
